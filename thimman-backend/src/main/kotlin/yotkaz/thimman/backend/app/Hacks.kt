package yotkaz.thimman.backend.app

import java.util.*

const val JPA_EMPTY_CONSTRUCTOR: String = "JPA needs empty constructor.";

// DEFAULT VALUES
const val DEFAULT_STRING = "";
val DEFAULT_DATE = Date(0);